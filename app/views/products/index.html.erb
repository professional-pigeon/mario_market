<div class="jumbotron">
  <h1>Welcome to Mario's International Curiosities</h1>
</div>

<% if @products.any? %>
  <div class="row">
    <div class="col-md-3">
    <h4>Here is a list of all of our products</h4>
      <ul>
        <% @products.each do |product| %>
          <li><%= link_to product.name, product_path(product) %></li>
        <% end%>
      </ul>
    </div>
    <div class="col-md-3">
      <div class="panel">
        <h4 class="panel-heading">Made in the USA</h4>
        <div class="panel-body">
          <% @local.each do |product| %>
          <p><%= link_to product.name, product_path(product) %></p>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="panel">
        <h4 class="panel-heading">Most Reviewed Products</h4>
        <div class="panel-body">
          <% @reviewed.each do |product| %>
          <p><%= link_to product.name, product_path(product) %></p>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="panel">
        <h4 class="panel-heading">Most Recently added</h4>
        <div class="panel-body">
          <% @recent.each do |product| %>
          <p><%= link_to product.name, product_path(product) %></p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <% else %>
    <p>No products have been created yet</p>
  <% end %>



<p><%= link_to "Add a Product", new_product_path %></p>
